# Баланс и формулы (v1.4 + геометрия v1.5)

Связано: [[00 — Главная (MOC)]], [[02 — Core loop и улучшения дыры]], [[03 — Prestige и прогрессия]], [[04 — Планета — стадии, события, объекты]]

## Геометрия и спавн (v1.5)

- Числовые параметры **\(R_{\mathrm{out}}\)**, смещение дыры относительно звезды, начальные скорости спавна на границе — задаются в коде и калибруются так, чтобы выполнялся целевой контраст «система ≫ горизонт» и читаемые траектории.
- Частоты и таблицы MP ниже остаются в силе; меняется **где** и с каким вектором появляется объект (у периметра системы, движение внутрь объёма).
- Реализация фазы 1: \`src/game/balance/layoutViewport.ts\` — \`BH_ORBIT_RADIUS_FRACTION\`, \`BH_SCREEN_ANGLE_RAD\`, \`SYSTEM_OUTER_RADIUS_FRACTION\`, \`STAR_COLLISION_RADIUS_FRACTION\`, \`STAR_DISPLAY_RADIUS_FRACTION\`; \`SimLayout.bh\`, \`systemRadius\`, \`starCollisionRadius\`; спавн и случайная скорость на границе — \`spawnOutsideGravity\` / \`spawnShip\`, \`randomBoundaryVelocity\` в \`src/game/simulation.ts\`; побег за \`systemRadius\` и **поглощение звездой** при \(r <\) \`starCollisionRadius\`; дампинг — \`VELOCITY_DAMPING\` в \`simulationPhysics.ts\`.

## Спавн объектов

- Базовая частота: **2 объекта/сек** в коде (\`BASE_SPAWN_PER_SECOND\`, до модификаторов); потолок поля — \`MAX_OBJECTS_ON_FIELD\`.
- Веса (сумма 100%):
  - 55% — мелкие астероиды (**1-3 MP**)
  - 25% — крупные астероиды (**10-15 MP**)
  - 15% — мусор/спутники (**20-35 MP**, могут давать бусты)
  - 5% — корабли/планетоиды (**50-80 MP**, могут давать бусты)

Во всех диапазонах MP используется равномерное целое распределение.

## Бусты

- Длительность: **45 секунд**
- Не стакаются (новый заменяет старый)
- Примеры: x1.6 MP, x2.0 скорость спавна

## Формула Prestige

- Доступ: `currentMP >= 10000`
- PP: `Math.floor(Math.sqrt(currentMP / 1000))`

## Оффлайн

- Множитель: **0.75**
- Кап: **12 часов**
- Брать скорость из сохранённого состояния:
  `offlineMP = onlineRateFromSavedState * secondsOffline * 0.75`

## 11 улучшений (цены и эффекты)

- Базовая кривая цены: рост **×1.45** за уровень (\`UPGRADE_COST_MULTIPLIER_PER_LEVEL\` в \`balance/upgradeEconomy.ts\`; диапазон x1.45–x1.6 в ТЗ — при необходимости разные множители по веткам).
- Уровни без лимита.

### Ветка A — Сингулярность

| # | Улучшение | Эффект |
| --- | --- | --- |
| 1 | Радиус Шварцшильда | +12% массы/горизонта |
| 2 | Радиус притяжения | +18% дальности гравитации |
| 3 | Аккреционный диск | +8% глобального MP |
| 4 | Релятивистские джеты | 25% шанс x1.8 MP на 20 сек + толчок к дыре |
| 5 | Гравитационное линзирование | +12% шанс редких объектов |
| 6 | Хокингово излучение | Пассивный MP-тик, растущий с массой |

### Ветка B — Окружение (на выбранную систему)

| # | Улучшение | Эффект + риск |
| --- | --- | --- |
| 7 | Гравитация главной звезды | +20% комет/астероидов, риск срыва орбит |
| 8 | Туманность системы | +15% газа, больше мелких объектов |
| 9 | Приливные силы планет | +25% MP от планетных объектов, риск разрушения |
| 10 | Приближение соседней галактики | +30% редких объектов, новые траектории |
| 11 | Сверхновая в системе | Большой спавн + временный x3 MP + мини-дыра |

## Планета: динамическая стоимость ускорения

- База: `100 MP = 1 мин`
- Множитель:
  `1 + (sumDeviation6PlanetParams / 100)`
- Типичный диапазон: `x1.0 ... x2.5-3.0`

## Требование синергии

Рост массы дыры и рост гравитации звезды должны совместно менять орбиты и экономику (доход + риск потери планет).
